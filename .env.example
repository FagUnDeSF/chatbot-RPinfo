# Copy to .env only in an authorized local environment.
# Do not commit .env or real secret values.

APP_NAME=chatbot-RPinfo
APP_ENV=local
APP_VERSION=0.1.0

# Secret locations only. Values must be provided by local env, vault or CI secrets.
# ERP_TESTE_DATABASE_URL=
# AI_PROVIDER_API_KEY=

